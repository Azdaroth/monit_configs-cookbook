check process mongodb with pidfile <%= node["mongodb"]["config"]["dbpath"] %>/mongod.lock  
  start program = "/etc/init.d/mongod start"
  stop  program = "/etc/init.d/mongod stop"
  if 5 restarts within 5 cycles then timeout